<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.beautyhair">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeautyHair">
        <activity
            android:name=".CustomerServiceRatingActivity"
            android:exported="true" />
        <activity
            android:name=".CustomerBookingSuccessfulActivity"
            android:exported="true" />
        <activity
            android:name=".CustomerBookingActivity"
            android:exported="true" />
        <activity android:name=".CustomerShopsListingActivity"></activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".GreetingActivity" />
        <activity android:name=".StartUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomerMainActivity" />
        <activity android:name=".ShopkeeperMainActivity" />
    </application>

</manifest>